# Moved to root docker-compose.yml — use `docker compose up` from project root.
# This file is kept to avoid breaking existing workflows.

services:
  trading-ui:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
